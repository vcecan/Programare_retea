Welcome to DNS application! \nfor begining here are disponible commands:\n'
      f'* /commands - show list of commands\n'
      f'* /resolve_domain / resolve_ip> - show a list of ip adresses assignet to domain or a list of domains assignet to ip adress\n'
      f'* /use_dns<ip> switch DNS server for browsing precedent commands')

tamap /resolve_domain,dupa apasam enter si scrim domainul pentru a rezolva domainul
tapam /resolve_ip,apasam enter, dupa asta ip-ul pentru a resolva ip

tapam /use_dns ,apoi adresa dns-ului necesar 

pentru a ne intoarce la cel de baza scrim /use_dns apoi default